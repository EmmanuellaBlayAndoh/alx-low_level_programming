#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - numbers
 * Return: Always 0
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
